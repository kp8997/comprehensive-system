source
    list.stream()
    Arrays.stream()

immediate operation
    map
    flatMap 
    distince
    filter
    sorted
    limit
    skip
    
terminal operation
    collect
    forEach
    reduce
    count

avoid primitive stream, instead we have autoboxing for primitive type stream
